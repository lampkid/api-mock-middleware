{
  "name": "api-mocker-middleware",
  "version": "1.4.4",
  "description": "a api mock middleware for webpack-dev-server or express",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/lampkid/api-mock-middleware.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib --copy-files",
    "prepublish": "npm run build"
  },
  "keywords": [
    "react",
    "create-react-app",
    "webpack-dev-server",
    "webpack"
  ],
  "author": "lampkid",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.2",
    "chokidar": "^2.0.3",
    "http-proxy": "^1.16.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0"
  }
}
